<%= form_with(model: poker_player, local: true) do |form| %>
  <% if poker_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poker_player.errors.count, "error") %> prohibited this poker_player from being saved:</h2>

      <ul>
      <% poker_player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :poker_player_name, class:"form-control" %>
    <br>
  </div>

  <div class="field">
    <%= form.label :nickname %>
    <%= form.text_field :nickname, id: :poker_player_nickname, class:"form-control" %>
    <br>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :poker_player_title, class:"form-control" %>
    <br>
  </div>

  <div class="actions">
    <%= form.submit "Registrar pokarista", class:"btn btn-success" %>
  </div>
  <br>
<% end %>
